["1. Replace the `javax.ejb` import statement with `jakarta.ejb` on lines 6 and 7.\n2. Replace the `@Remote` annotation with `@jakarta.ws.rs.Path(\"<endpoint>\")` and add an endpoint path on line 12.\n3. Remove the `@Stateless` annotation on line 11.\n4. For every public method on the EJB being converted, annotate the method with `@jakarta.ws.rs.GET` and `@jakarta.ws.rs.Path(\"<endpoint>\")`.\n5. Add `@jakarta.ws.rs.QueryParam(\"<param-name>\")` to any method parameters if needed."]